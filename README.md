# Western Highways — Prototype TMA Configurator

Open `index.html` in a browser to preview the site. The `swatches.json` file contains selectable color swatches used by the sample preview box.

Next steps:
- Provide an exported glTF/GLB of the assembly and I will integrate a Three.js viewer and map swatches to materials.
- Or tell me which UI options (accessories, dimensions) to add next.
